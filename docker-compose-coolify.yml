services:
  simpleserver:
    image: jafber/frogwatch-simpleserver
    build:
      context: ./simpleserver
      dockerfile: Dockerfile
    restart: always

  front:
    image: jafber/frogwatch-front
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
